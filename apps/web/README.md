# MINAN Web

Next.js 16 App Router frontend for the public storefront, checkout lead flow, analytics clients, and role-gated admin UI.

All API traffic should go to the Express API through `NEXT_PUBLIC_API_URL`. Do not add Next.js route handlers under `app/api`.

